<h4>Categorias: </h4>
<div class="card">
    <div class="card-body">
        {{#each categorias}}
            <a href="/categorias/{{slug}}"><h5>{{nome}}</h5></a>
            <hr>
        {{else}}
            <h5>Nenhuma categoria cadastrada!</h5>
        {{/each}}
    </div>
</div>